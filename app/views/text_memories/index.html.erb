<p id="notice"><%= notice %></p>

<h1>Text Memories</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Text</th>
      <th>Date</th>
      <th>Lat</th>
      <th>Lon</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @text_memories.each do |text_memory| %>
      <tr>
        <td><%= text_memory.title %></td>
        <td><%= text_memory.text %></td>
        <td><%= text_memory.date %></td>
        <td><%= text_memory.lat %></td>
        <td><%= text_memory.lon %></td>
        <td><%= link_to 'Show', text_memory %></td>
        <td><%= link_to 'Edit', edit_text_memory_path(text_memory) %></td>
        <td><%= link_to 'Destroy', text_memory, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Text Memory', new_text_memory_path %>
