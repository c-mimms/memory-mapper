<%= form_for(text_memory) do |f| %>
  <% if text_memory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_memory.errors.count, "error") %> prohibited this text_memory from being saved:</h2>

      <ul>
      <% text_memory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="field">
    <%= f.label :lat %>
    <%= f.text_field :lat %>
  </div>

  <div class="field">
    <%= f.label :lon %>
    <%= f.text_field :lon %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
